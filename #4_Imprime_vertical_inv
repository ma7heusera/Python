def imprime_vertical_inv_num (num):
    if num < 10:
        print (num)	
        return
    print (num % 10)
    imprime_vertical_inv_num (num // 10)

n=int(input('Número inteiro positivo?'))
imprime_vertical_inv_num(n)
